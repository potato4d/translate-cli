# Release 0.1.5

## Objective

Publish local LLM support added after `v0.1.4`.

## Acceptance checklist

- [x] `translate-cli` and `xtask` package versions are `0.1.5`.
- [x] `Cargo.lock` workspace package versions are `0.1.5`.
- [x] Standard local validation passes.
- [x] Version bump commit is pushed to `master`.
- [x] Tag `v0.1.5` is pushed and release workflow completes.
- [x] Main repo release has all platform archives and `checksums.txt`.
- [x] Homebrew tap release and Formula point to `0.1.5`.
- [x] Homebrew install/upgrade path prints `translate-cli 0.1.5`.

## Version scope

Changes since `v0.1.4`:

- Ollama adapter and model detection.
- LM Studio adapter through `lms`, including `--tool lms` alias.
- Setup recommendation updates: runnable Ollama first, then LM Studio, then existing Agent CLIs unless existing config or `TRANSLATE_CLI_TOOL` overrides it.
- Local model config and env overrides for Ollama and LM Studio.
- Wizard guidance for installed local LLM tools without downloaded models.
- README, architecture note, and fake CLI coverage for local LLM support.

## Local validation

- `cargo fmt --check`: passed.
- `cargo test --workspace`: passed. Unit tests 19, integration tests 10.
- `cargo build --release`: passed.
- `./target/release/t --version`: `translate-cli 0.1.5`.
- `cargo run -p xtask -- build-release`: passed and generated the current `darwin-arm64` archive locally.
- `tar -xOf dist/t-darwin-arm64.tar.gz t | strings | rg 'translate-cli 0\.1\.5|ollama|lmstudio|Translate CLI setup'`: release binary contains the expected version, setup text, and local LLM tool strings.
- `tar -xOf dist/t-darwin-arm64.tar.gz README.md | rg -n 'Ollama|LM Studio|--tool <codex\|claude\|ollama\|lmstudio>|TRANSLATE_CLI_OLLAMA_MODEL'`: release README contains local LLM documentation.
- `rg --files dist | sort`: local host archive, staged README, staged binary, and `checksums.txt` generated.
- `rg -n "node|Node|depends_on" dist`: no Formula was generated from the one-platform local archive; staged README mentions that Node.js is not required.
- `git diff --check`: passed.

## Remote verification

- Version bump commit `b8c542b` was pushed to `master`.
- CI run `26615254458` completed successfully for `b8c542b`.
- Tag `v0.1.5` was pushed to `origin`.
- Release workflow run `26615273052` completed successfully for tag `v0.1.5`.
- Main repo release `v0.1.5` was published with all required assets:
  - `t-darwin-amd64.tar.gz`
  - `t-darwin-arm64.tar.gz`
  - `t-linux-amd64.tar.gz`
  - `t-linux-arm64.tar.gz`
  - `t-windows-amd64.zip`
  - `checksums.txt`
- The release workflow skipped Homebrew tap steps because `HOMEBREW_TAP_TOKEN` was not present in Actions.
- Published tap release `translate-cli-v0.1.5` manually in `potato4d/homebrew-tap` with the same six assets.
- Generated `dist/homebrew/Formula/translate-cli.rb` from the `v0.1.5` release assets.
- Pushed Homebrew tap commit `95db5b7` updating `Formula/translate-cli.rb` to version `0.1.5`.
- Verified the remote Formula:
  - `version "0.1.5"`
  - URLs point to `translate-cli-v0.1.5`
  - no `node`, `Node`, or `depends_on` entries.
- `brew update` reported `potato4d/tap` updated.
- `brew info potato4d/tap/translate-cli` reported `0.1.4 -> stable 0.1.5`.
- `brew upgrade potato4d/tap/translate-cli` upgraded the local install from `0.1.4` to `0.1.5`.
- `/opt/homebrew/opt/translate-cli/bin/t --version`: `translate-cli 0.1.5`.
- `brew list --versions potato4d/tap/translate-cli`: `translate-cli 0.1.5`.
- `brew test potato4d/tap/translate-cli`: passed.

## Completion audit

- Source version is published: `master` contains commit `b8c542b` with package version `0.1.5`.
- CLI release is public: GitHub release `v0.1.5` exists with all configured platform archives and `checksums.txt`.
- Release artifacts cover the full `.github/workflows/release.yml` matrix.
- Homebrew distribution is public: tap release `translate-cli-v0.1.5` exists, and the tap Formula points at its assets.
- Formula remains native-binary only: no Node.js dependency is present.
- End-user install path works: Homebrew upgraded and the installed binary prints `translate-cli 0.1.5`.
